{
  "name": "sample-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node src/app.js",
    "test": "jest --ci --reporters=default --reporters=jest-junit",
    "build": "echo \"build step (placeholder)\" && mkdir -p build && cp -r src/* build/",
    "lint": "eslint src/**/*.js"
  },
  "devDependencies": {
    "@eslint/js": "^9.38.0",
    "eslint": "^9.38.0",
    "globals": "^16.4.0",
    "jest": "^29.0.0",
    "jest-junit": "^13.0.0",
    "supertest": "^7.1.4"
  }
}
